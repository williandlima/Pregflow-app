<resources>
    <!-- Tema base da splash/janela antes do Compose assumir; as cores reais
         de light/dark theme vivem em ui/theme/Theme.kt (Material 3). -->
    <style name="Theme.PregFlow" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">#7C3AED</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
